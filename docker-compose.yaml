version: '2'

services:

    postgres-dev:
        image: postgres
        restart: always
        ports:
            - 15432:15432
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: ar3nda1

    postgres-unittest:
        image: postgres
        restart: always
        ports:
            - 25432:25432
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: ar3nda1
